import { StyleSheet } from "react-native";

const Typography = StyleSheet.create({
  // headings
  heading_1: {
    fontFamily: "Bold",
    fontSize: 48,
  },
  heading_2: {
    fontFamily: "Bold",
    fontSize: 40,
  },
});

export default Typography;
